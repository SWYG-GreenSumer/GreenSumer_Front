import React from 'react'
import BoardTitle from './BoardTitle'

type Props = {}

const ReviewBoard = (props: Props) => {
  return (
    <div className=''>
      <BoardTitle>ReviewBoard</BoardTitle>
    </div>
  );
}

export default ReviewBoard;
